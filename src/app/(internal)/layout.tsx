export const metadata = {
  title: "SunLeads AI — Internal",
  robots: "noindex, nofollow",
};

export default function InternalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
